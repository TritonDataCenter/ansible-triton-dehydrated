---
galaxy_info:
  role_name: triton_dehydrated
  namespace: joyent
  author: Joyent (joyent.com)
  description: Generate Let's Encrypt Certificates with triton-dehydrated
  company: Joyent
  issue_tracker_url: https://github.com/joyent/ansible-triton-dehydrated/issues
  license: MPL-2.0

  min_ansible_version: 2.9

  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:

  #
  # Provide a list of supported platforms, and for each platform a list of versions.
  # If you don't wish to enumerate all versions for a particular platform, use 'all'.
  # To view available platforms and versions (or releases), visit:
  # https://galaxy.ansible.com/api/v1/platforms/
  #
  platforms:
    - name: Alpine
      versions:
        - all
    - name: CentOS
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: FreeBSD
      versions:
        - all
    - name: NetBSD
      versions:
        - all
    - name: OpenBSD
      versions:
        - all
    - name: SmartOS
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
    - name: Void
      versions:
        - all

  galaxy_tags:
    - cloud
    - dehydrated
    - letsencrypt
    - ssl
    - sslcert
    - sslcertificate
    - sslcertificates
    - tls
    - certificate
    - certificates

dependencies: []
